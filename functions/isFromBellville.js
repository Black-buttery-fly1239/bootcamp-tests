function isFromBellville(vehicle) {
    if (vehicle.startsWith("CY")) {
      return true;
    } else { 
      return false }
  };
  console.log(isFromBellville("vehicle"));

  function isFromBellville(vehicle) {
    if (vehicle.startsWith("CY")) {
      return true;
    } else { 
      return false }
  }; 